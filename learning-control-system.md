# 学習コントロール用システム設計

## 1. システム全体の考え方
- **ガイド役**: Duolingo風に「いまやること」を軽く提示するが、やらない日が続いても罰しない。
- **非強制・低摩擦**: 記録は1行メモで済ませ、週単位で眺めるだけ。毎日実行を前提にしない。
- **偏りを“知らせる”だけ**: 接触:体験の目安 7:3 をメーター表示するが、守れなくてもOK。
- **変化より連続性**: 週ごとにテーマや傾向を言語化して NotebookLM に渡し、ゆるく次の一歩を作る。

## 2. 構成要素（必要な項目・状態・ログ）
- **状態**
  - インプット比率メーター: 直近7日で接触/体験/理解の件数。
  - 今日のミッション: 5〜10分で終わる接触 or 体験のワンステップ。
  - テーマ候補リスト: 気になった題材・ツール名をストック。
- **ログ項目**
  - 日付
  - インプット種別（接触/体験/理解）
  - 内容（リンク・スクショ・試したことの一言）
  - 任意メモ（気づき・「よくわからない」などもOK）
- **週次メモ**
  - 件数サマリ（接触・体験・理解）
  - 偏りコメント
  - 来週まわすテーマ（1〜3件）

## 3. 学習の流れ
### 1日単位
1. **今日のミッションを1つだけ選ぶ**（接触 or 体験、5〜10分）
2. **終えたら1行でログを残す**（フォーマットは後述）
3. **余力があればテーマ候補を1件メモ**（ストックだけ、調べない）

### 1週間単位
1. 7日分のログを眺め、件数をカウント（接触/体験/理解）。
2. 偏りチェックルールを当て、やさしいフィードバック文を出す。
3. NotebookLM に入れる週次まとめを作成。
4. 次週に軽く回すテーマを1〜3件だけ選ぶ。

## 4. インプット記録フォーマット（1行）
```
YYYY-MM-DD | 接触/体験/理解 | 内容（リンク・スクショ名・試した操作） | メモ（任意）
```
- 例: `2024-05-10 | 体験 | Claudeに自分の議事録を投げて要約させた | トーン調整がうまくいった`

## 5. 偏りチェックの簡単なルール
- 直近7日での件数を `C=接触, T=体験, U=理解` とする。
- **比率目安**: C:T ≈ 7:3（理解はカウントのみ、目標なし）。
- **判定**
  - 体験不足: `T < C * 0.3` または `T <= 2`
  - 接触偏り: `C > T * 3` かつ `C >= 5`
  - 休息多め: `C+T < 4`（低負荷でOKのサイン）
- **表示**
  - メーター例: `接触 ████████ 8件 | 体験 ███ 3件 | 理解 █ 1件`

## 6. ユーザーに返すフィードバック文の例
- 体験不足: 「接触がリード中。今日のミッションは**体験5分**にしてみる？」
- 接触偏り: 「リンク収集中でいい感じ。週に1回だけ、1本手を動かすとバランス取れそう。」
- 休息多め: 「今週は軽め。余裕が出た日に1ミッションだけ足してもOK、無理は不要。」
- バランス良好: 「接触と体験がほどよく混ざってる。続けてOK。」

## 7. 今日のミッション生成（例）
- **接触ミッション（5〜10分）**
  - 「Twitterで"LLM プロンプト"を5件流し見して1件スクショ」
  - 「YouTubeで最新AIデモを1本倍速で見る」
- **体験ミッション（5〜10分）**
  - 「自分のメモをChatGPTに投げて、見出しだけ返してもらう」
  - 「画像生成で“今日の気分”を1枚作ってみる」
- 成否は不問、深掘り禁止。終えたら1行ログを残すだけ。

## 8. 週次まとめ生成（NotebookLMに渡す文章テンプレ）
```
【今週のインプット件数】
- 接触: X件 / 体験: Y件 / 理解: Z件

【気づいたこと】
- （任意、なければ「特になし」）

【次に回すと良さそうなテーマ（1〜3件）】
- 例: 「音声→テキスト要約を他サービスでも試す」
- 例: 「画像生成のプロンプト短縮を調べる」
- 例: 「ワークフロー自動化ツールを1つ触る」
```
- 数字と短文だけでOK。偏り指摘は「次の一歩」提案として書く。

## 9. 運用ルールの再掲
- 毎日やらなくていい。1日1ミッションで十分。
- 記録は雑でOK。評価は週単位。
- 偏りに気づくためのメーターであり、強制はしない。

## 10. 付録: 実行用シンプルCLIの使い方
`learning_control.py` で上記の流れをそのまま動かせます（追加ライブラリ不要）。

- ミッション生成（接触/体験を指定、未指定ならランダム）
  ```bash
  python learning_control.py mission --type contact
  ```
- 1行ログを追加（`input-log.csv` が自動生成されます）
  ```bash
  python learning_control.py log --type experience --content "Claudeで議事録1件要約" --memo "トーン良さげ"
  ```
- 直近7日の偏りチェックと週次まとめ文を表示
  ```bash
  python learning_control.py summary --days 7 --themes "画像生成の短縮プロンプト" "音声→テキスト別サービス"
  ```

出力に NotebookLM に貼れる週次テンプレも含まれるので、そのままコピペできます。
